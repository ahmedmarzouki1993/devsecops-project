{
  "mcpServers": {
    "sequential-thinking": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "mcp-sequentialthinking-tools"],
      "_comment": "Helps Claude break down complex multi-step tasks like debugging CI pipelines or planning Terraform changes"
    },
    "kubernetes": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-kubernetes"],
      "_comment": "Direct kubectl access — Claude can inspect pods, logs, events, deployments in AKS"
    },
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-server-filesystem", "/home"],
      "_comment": "Scoped file access for reading/writing project files, Terraform state, K8s manifests"
    }
  }
}
